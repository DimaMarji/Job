export const buttonTypes =
    [
        "primary", "secondary", "ghost", "dashed", "link", "text"
    ] as const

export enum ButtonTypes {
    primary = "primary",
    secondary = "default",
    ghost = "ghost",
    dashed = "dashed",
    link = "link",
    text = "text",
}
