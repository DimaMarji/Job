// Like as base URL and request header …
const config = {
    baseUrl: "https://websiteback.zcoderz.com/api/v1/",
    headers: {
        //  Authorization: `Bearer ${readCookies().KEY_TOKEN}`,
    },
};
export default config;
